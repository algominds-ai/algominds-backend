1. **(a) only: 2,500 characters per requirement’s proving passage; retain 4,000-character homepages, batch 2 and the 90-second timeout.** Use the existing proving highlight plus surrounding context—not the first 2,500 characters of the page. Preserve attribution, negation and relevant dates; keep full pages in evidence storage.

   Apply the same excerpt wherever that evidence appears in the judge payload, including `evidenceQuote` and `pageEvidence`; otherwise duplicated full text defeats the cap. AlgBra’s homepage rescue stays intact. My batch-size recommendation attacked the multiplier before identifying the oversized input; that was insufficient.

2. **Yes—with stronger acceptance checks.** Replay all ten captured Form3 rows, including unchanged homepages, twice with cache misses. Require every slice below 90 seconds without retries, independently accepted controls retained, independently rejected controls refused, and each grounding quote genuinely supporting its requirement. “Unchanged decisions” alone preserves existing mistakes.

   Then Form3 ×5 live, **up to $2**, requiring five accepted companies, no group duplicates, completed people stage and total below 600 seconds. Replay costs belong in the budget. Passing this screen supports confirmation; it does not prove the timeout problem cannot recur.

3. **Hold—the drafted sentence is not ready for confirmation.** “Only when nobody holds…” makes absence of a senior candidate permission to select an otherwise unsuitable subordinate; absence from the roster also does not establish absence from the company. Conversely, owning the *whole function* could wrongly exclude a legitimate workflow-specific buyer.

   Use: **“An inferred owner must satisfy the rubric’s decision or budget authority; a matching sub-function title alone does not establish that authority.”**

   Replay the affected saved roster plus one unrelated workflow-specific rubric before inclusion. Require the labelled accountable buyer—not automatically the VP Growth title—and rejection of the labelled sub-function false positives. Otherwise freeze the selector for confirmation.


