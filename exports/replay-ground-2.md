# Judge replay 2 on p2/final 777416a (required quote, batch 4), 11 rows, $0.143, 4 slices 17/49/12/47 s, no retry
PASS 5: steadypay reject (r2 contradicted on the LaaS sentence), gettongo reject (r2 unproven, r4 proven "funding raised 10000000 USD"), hioscar reject, skyscanner retain, dexcom retain.
r4 (funding/revenue band) now grounded with a verbatim record-line quote for every Ondato row. r2 (self-serve) stays unproven for kontigo, paymiq, ziglu, peymo because the replay cannot see the homepage passage (never persisted). algbra.com: r2 CONTRADICTED on "strategic network of tier 1 global partners" = the cherry-picking risk codex named; the live screen must show algbra-class direct-and-partner companies stay accepted, else the instruction needs work. sennder retained again (r1 unproven, not contradicted; the fixture now makes r1 strict, which blocks the unproven route in the next live run).

## Captured Form3 rows, batch 1 (exact live inputs, $0.38): grounding refusals are CORRECT
10/10 single-row calls under 90 s (10-57 s). Only appdirect retained. For the 8 r5-unproven rows the stored page passages never state production Kubernetes use (product copy, a Go monolith at 1Password, a generic "Kubernetes containers" list item at Keeper, a vendor's platform at GoCardless). arctera contradicted as an acquired subsidiary. So the old engine was storing Form3 rows on proof pages that did not prove r5; grounding refuses them. Form3 yield will fall until the search finds real evidence; that is the honest number.
