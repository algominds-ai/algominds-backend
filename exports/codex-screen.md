1. **(c)—batch 4 → 2.** Preserve the homepage evidence that rescued AlgBra. Cutting it risks undoing the quality gain; switching models changes the judge itself. A 180-second timeout also conflicts with the existing five-minute judge/round ceilings once the internal retry and earlier work are included.

   Quality cost: smaller slices further reduce cross-row organisation deduplication; operational cost: more concurrent requests and repeated instructions. Homepage size is a plausible cause, **not yet a proven one**.

   Cheapest screen: replay the **actual captured Form3 inputs, including homepages**, twice at batch 2 with cache misses; then one Form3 ×5 live run only if both pass, **$2 combined ceiling**. Require zero timeouts/retries, preserved labelled decisions, no duplicate groups, and live completion within 600 seconds. Two passes support promotion to confirmation—not a reliability guarantee.

2. **Yes—persist failed-round spend; no—it is not merely a close-handler edit.** The ledgers are local and merged only on successful returns; `close-errored` cannot save entries it never receives. Completed-call costs must survive failure and be persisted without double counting; aborted-call charges remain unknown until billing reconciliation.

   Include it without a separate **paid** screen, but require a fault-injection test: successful paid stages → judge throws → known costs survive; repeating error closure adds nothing. Do not report unknown charges as zero.

3. **No—not full promotion yet.** Ondato’s company result is meaningful positive evidence, especially AlgBra with its homepage. But Form3 still produces no usable run, other profiles remain untested on the assembled changes, and “likely 10/11” is not a labelled result. Even perfect person precision leaves **3/5 buyer coverage**. Keep grounding as the candidate; require seven-profile confirmation with the revised Form3 contract applied consistently to comparisons.

4. **Yes, with accounting and labelling first at zero vendor cost.** Then **up to $2** for the staged Form3 screen and **up to $6** for confirmation, leaving **$5.34 nominal reserve**. Reconcile hidden charges before allocating that reserve; $13.34 is not verified available spend.

   If Form3 fails again, **cancel confirmation**. Do not buy seven profiles’ worth of evidence that an already-failing build still fails.


