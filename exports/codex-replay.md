1. **Yes—for screening, not promotion.** Required `quote: string` removes omission as a valid output shape; it does **not** force evidence because `""` remains valid. Inspect raw statuses before grounding: “unproven with no quote” may be the model’s decision, not a downgrade caused by omission. Require a cheap replay demonstrating that the visible funding facts produce supporting quotes and correct r4 statuses. **No `basis` field:** another explanation is not another guarantee.

2. **Yes—partly informative.** Missing homepages invalidate the five *whole-company retention* expectations, but do not excuse missing r4 proof where funding facts are visibly supplied. My earlier unconditional demand that those anchors survive this incomplete replay was wrong.

   **Run the live screen after the schema/funding check**, with one minimal prerequisite: save the exact judge-visible inputs, including homepage passages and every requirement’s page evidence, before invoking the model. Do not require an impossible faithful reconstruction of lost historical evidence. Treat this run as both screening and evidence capture; do not promote from “better than incomplete replay.”

3. **Yes—make Form3’s listed vertical requirement strict.** If unknown vertical must not reach the delivered list, non-strict r1 encodes the wrong acceptance contract. Strictness blocks Sennder’s `unproven` route; it still cannot stop a false `proven` with an irrelevant quotation.

   Keep this a separately named contract revision, freeze it before measuring, and rescore both comparison arms identically. Do not claim its benefit as an engine-only improvement or require every listed-category requirement everywhere to become strict.

4. **Yes—with the cheap schema check first and faithful evidence capture before paid integration.** The 21/37/34-second slices are encouraging, but missing homepage payloads mean they have **not** cleared the live timeout risk.

   - **Up to $2 combined:** schema/funding replay and timeout screening with representative complete payloads; zero-cost tests first for both retry boundaries.
   - **Up to $3:** Ondato ×5 + Form3 ×5 live, with exact-input capture.
   - **Up to $6:** seven-profile confirmation only after both pass, on frozen code, contracts and keys.
   - **$3.48 reserve:** billing uncertainty or one targeted diagnostic.

   A failed screen stops progression. Spending the remaining budget is not the objective.


