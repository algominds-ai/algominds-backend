# Why hioscar.com, sennder.com (form3) and steadypay.co (ondato) were accepted (final run p2final2)

hioscar.com and sennder.com: form3 requirement r1 (vertical list, proof `record`, not strict). The judge wrote the disqualification into fitReason ("Health insurance isn't among the listed reportable-downtime verticals" / "Freight forwarding isn't among the listed ... verticals") but classified r1 as `unproven`, not `contradicted`. `keepsRow` (src/core/companies/judge.ts:283-295) refuses only `contradicted`; `unproven` blocks only when the requirement is in `mustBeProven` (src/core/requirements.ts:17-25: proof `page` or strict). So an affirmative non-listed vertical passed. Defect class: permissive decision logic plus an under-called judge classification.

steadypay.co: ondato r2 (strict, self-serve signup). Strict, so `unproven` would block; the judge marked it `proven` ("UK-based consumer lending app with self-serve app signup") although the description says "Lending as a Service platform ... allowing you to offer tailored credit services", an embedded/B2B2C signal r2 excludes. Defect class: mistaken interpretation.

Minimal correction (NOT applied to p2/final; no eval proof yet): one sentence in JUDGE_INSTRUCTIONS (src/core/companies/judge.ts:59-81): a hard record requirement that lists qualifying categories is `contradicted`, not `unproven`, when the record affirmatively states a different, non-listed category; silence stays `unproven`. No blacklist, no strictness change, mustBeProven untouched.
Proof pair that must stay accepted: skyscanner.net (Travel metasearch, listed) and dexcom.com (Digital health, listed). Ondato contrast: check algbra.com / kontigo.lat industry by hand first.
Next paid screen (codex): form3 + ondato, count 5, baseline and treatment once each, $8 ceiling.

Drafted, gated, unmerged: commit 2f17a23 on p2/judge (gate PASSED, 605 tests). Screen it with: from .claude/worktrees/p2-judge, `SFW_SHIM_DISABLE=1 bun eval/run.ts --arm p2judge --trials 1 --count 5 --profile form3 --port 8808`, then the same for ondato; seed keys on p2-base, label, `bun eval/rescore.ts --arm p2judge`; promote only if hioscar/sennder/steadypay-class rows drop while skyscanner/dexcom stay accepted.

## Ondato strict r2 (self-serve signup): steadypay.co and gettongo.com
steadypay.co description says embedded / Lending-as-a-Service / white-label only; no "signup" or "self-serve" word. Judge fitReason: "consumer lending app with self-serve app signup" = invented. gettongo.com description is one sentence with no channel language; the judge itself wrote "no clear signup or verification detail" and still marked r2 proven. The judge sees r2's exclusion clause verbatim. So: one contradiction (steadypay) and one fabrication from silence (gettongo). A channel sentence would fix only steadypay. Evidence-line grounding (a strict requirement is proven only by a verbatim quote found in the row's evidence) refuses both. Near-neighbours that keep an affirmative direct-consumer line to quote and must stay accepted: algbra.com, kontigo.lat, paymiq.com, ziglu.io, peymo.com. Keep the existing "a word like partners alone does not disqualify" clause.

## Form3 hang (p2final3): reconciliation
No Exa agent-run id is persisted anywhere (startAgentRun's id lives only in the step closure), so the vendor-side state of the runs started after round 2 cannot be checked. Rounds 1 and 2 completed in about 90 s each; last DB write 22:55:28 IST; error at 23:10:58; 14.5 min of silence, no round 3 row. Cost accounting gap: getAgentRun reports cost to the ledger only on status completed, so an in-flight run at failure time is unrecorded. Worst-case unrecorded exposure for one round: about 11 angles at $0.025-$0.10 each, about $1.

## Form3 logged run (p2form3-log): where the time goes
Route: Exa fast search (not the paid agent). 10 angles x 100 results = ~734 unique candidates; 722 rejected by the local entity-bounds filter (107 of the 120 stored samples: headcount below 501), 12 to the judge, 5 found. The 722 rejects cost nothing per row. Timings: synthesize 31 s + 10 searches (~1 s each) appear TWICE in the round; the two judge slices took 480 s and 190 s (more than the 534 s wall clock), so the judge timeouts caused a rerun of synthesize + search (paid). The judge is the time and cost driver, not the fan-out. filterEntities applies only the first angle's bounds to all angles (harmless while bounds are identical).

## SteadyPay, replayed on the candidate (grounded judge): a mixed-channel case, not a hallucination
r2 proven with the verbatim homepage quote "Sign Up to CashWave through the SteadyPay app: Install the SteadyPay app and access up to £500, with no added interest." The record's description separately states embedded lending / LaaS / white-label for other businesses. The judge did not reconcile the two. SteadyPay runs BOTH a consumer app with self-serve signup and an embedded B2B line. The key rejects it on the embedded line; the judge accepts it on the consumer app. This is a contract question (does a mixed-channel company with a real consumer self-serve app qualify for r2?), to be adjudicated with the homepage evidence in view, not an engine bug.
