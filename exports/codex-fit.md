1. **Yes—persist raw judge output and post-grounding statuses before changing judgment again.** A replay may produce a different quote; it cannot establish why the original acceptance happened.

   Require the model to reconcile supporting and contrary evidence for the **same entity and requirement**. But “partners” alone is not contradiction, and another sentence is not a demonstrated fix. Test the actual failure against frozen direct-only, partner-only and mixed-channel controls. **Do not add a worker entailment judge yet:** it repeats the same semantic task with another potential failure mode. Presence checking never guaranteed truth; my earlier recommendations should have made that limitation more prominent.

2. **One diagnostic: manually label the exact post-dedupe, location-filtered roster handed to the selector, against that run’s rubric.** A qualifying candidate present but unselected identifies a selection failure; none present identifies inadequate roster coverage for the task—not proof that the company has no buyer. Company size alone decides neither.

   Also, your buyer-fit instrumentation is misleading: it saves only **dropped** picks, so no rows does not prove a positive assessment. Worse, the current code defaults a missing fit result to `"owner"` and bypasses checking when all picks self-report explicit matches. Fix that fail-open behavior before relying on this check.

3. **Fix and screen Ondato first; do not run both full draws regardless.**

   Order:

   - Finish and label the already-running Form3 run.
   - Zero-cost roster diagnosis; persist complete judge/buyer-fit decisions; correct missing-fit admission.
   - Uncached targeted semantic controls, then **one Ondato ×5 screen**, approximately $1.2—not a guaranteed invoice.
   - Require five accepted distinct companies, **all delivered people accepted**, coverage **≥4/5**, and frozen time/cost gates. Form3 must independently meet those quality bars and its 600-second ceiling.
   - Only then freeze the exact build for **two seven-profile draws**, count five, approximately $16 combined. No edits between draws; publish both.

   If a prerequisite fails, use the remaining allocation on its saved-input failure, not on calling an already-failing candidate “confirmed.” Reconcile actual charges throughout.

4. **7.7/10 conditional forecast; planning band 6.0–8.6, not a confidence interval.** Your assumed five profiles at 0.8–1.0 plus Form3 at 0.8–1.0 and Ondato at zero imply **6.86–8.57**; the wider downside allows another Form3 failure.

   **Empty rosters alone do not zero Ondato.** With valid deliveries, perfect person precision and three covered companies, its score could be 0.60. SteadyPay’s rejection currently triggers the zero. Two draws provide an observed range—not a calibrated noise band or evidence of scale.


