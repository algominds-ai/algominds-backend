1. **No to a blanket lookup and “3× means subsidiary”; yes to checking entity consistency across record and proof.** Lloyds exposes a structural defect: the engine combines one entity’s headcount with another entity’s operating evidence.

   Require the proof’s operator to match the candidate’s evaluated entity; a mismatch triggers targeted resolution or leaves the requirement unproven. Use headcount disagreement as another trigger, not proof of identity. Group membership alone neither invalidates an independently eligible subsidiary nor lets it borrow group-wide evidence. Test parent-proof/subsidiary-record pairs, valid subsidiary-specific proof, and unrelated similar names.

2. **(a) Not distinguishable from “no row”—and your evidence query may be wrong.** The current code saves the resolved fallback reply under ordinary evidence kind **`select`**, not `select-fallback`. Inspect that row, the merged/deduped candidates actually passed to selection, and step completion. A 25 kB raw roster says nothing about eligible candidates. Missing output is not demonstrated selector refusal.

   **(b) Yes, investigate before another draw—but this is not silent skipping.** You supplied a recorded missing-secret error, an empty identity result and a runtime/port failure. Preflight required secrets without printing them; isolate inspector ports; then trace whether Exa fallback had an organisation ID. Current code returns an empty fallback when that ID is absent. Distinguish configuration failure, unresolved identity and genuinely empty retrieval in the terminal outcome.

   **(c) Keep abstaining on the supplied description alone.** A YC company page is an acceptable corroborating source, but “co-founder” can remain historically true after departure. Inspect the actual passage: current affiliation plus Clay’s current-role record may support verification; merely naming a founder does not establish today’s Chief Business Officer role or buyer eligibility. Do not automatically override UNKNOWN based on the domain.

   **Also: 2/5 coverage alone scores 0.40, not zero**, assuming perfect precision and all gates passing. Identify the actual failed gate.

3. **A—repair the environment/accounting-of-outcomes first, then targeted entity consistency, then the two screens.** Not another universal identity heuristic.

   Spend approximately **$5 combined**, count five each; require five accepted distinct companies, all delivered people accepted, ≥4/5 buyer coverage and existing time/cost gates. Inspect missing-selection and verification failures from saved evidence before paying again.

   B buys a confounded number; C risks an incomplete draw after unproven fixes. Neither establishes quality.

   **Expected subsequent seven-profile rating: roughly 7/10, planning band 5.5–8.5**, conditional on these repairs working—not a measured rating. Option A itself produces two profile results, not a new overall score; the earlier build’s observed 6.29 remains historical, not transferable.


